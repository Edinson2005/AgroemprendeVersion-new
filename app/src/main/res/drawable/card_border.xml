<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Fondo de la tarjeta -->
    <stroke
        android:width="1dp"
        android:color="@color/colortexto"/> <!-- Color y grosor del borde -->
    <corners android:radius="10dp"/> <!-- Esquinas redondeadas -->
</shape>
